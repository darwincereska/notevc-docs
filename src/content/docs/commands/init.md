---
title: Init
---

## Initialize a NoteVC repository

When initializing a repository you have two options:

1. Initialize in the current directory
2. Initialize in a specified directory

### `notevc init [path]`
```bash title="Examples"
notevc init # Current directory
notevc init ./notes # Specified directory
```
